imports:
- path: create_vms.py

resources:
# referenced inside Python template as context.env['name']
- name: fox
  type: create_vms.py
  properties:
# referenced inside Python template as context.properties['zone']
    zone: us-east1-b